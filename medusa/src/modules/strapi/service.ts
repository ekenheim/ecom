export class StrapiClientService {
  // Intentionally empty — the loader initialises the @strapi/client instance
  // and registers it directly in the Awilix container as 'strapiClient'.
  // This class exists solely to satisfy the Module() service requirement.
  constructor(_container: unknown, _options: unknown) {}
}
